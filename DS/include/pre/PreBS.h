#pragma once
#include "tools/Number.h"

// The full name is: Binary Search.
// The algorithm can be ONLY implemented if the array is ALREADY sorted.
void PreBSEntryPoint(int, char* []);
int PreBinarySearch(const void*, void*, int, int, int, Compare);