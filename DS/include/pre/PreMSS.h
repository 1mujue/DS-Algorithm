#pragma once
// The full name is: Max Sum of Subsquent.
// The algorithm gives a way to caculate the max sum of an INTEGER array.
// Attention: the sub sequence can be NULL, so if the array is made by ONLY
// nagative numbers, then the answer should be 0.
void PreMSSEntryPoint(int , char* []);
int PreMSSSolution(int*, int, int);
int PreMSSDivideAndConquer(int*, int, int);
int PreMSSLinear(int*, int, int);
